import { Inter } from "next/font/google";

/** Used only by the invoice document — kept separate from the app's default font-sans. */
export const inter = Inter({ subsets: ["latin"], display: "swap" });
